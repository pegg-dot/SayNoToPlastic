# v40.10 Welcome Modal rendered-review source

- `before-v40.10.png` — user-provided local screenshot, 2048×1330, showing the v40.9 first-visit dialog exceeding the desired one-popup viewport experience.
- User direction: reduce the popup so the visitor can see the whole dialog without normal scrolling; retain the portrait film and existing behavior.
- Acceptance is not promoted to done until the v40.10 render is reviewed locally.
